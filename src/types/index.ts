export type Profile = {
    id: string;
    email: string;
    isActive: boolean;
    isEventOrganizer: boolean;
}

export type Option = {
    value: string | number;
    label: string;
};
